const Footer = {
    render() {
        // Footer desktop (opcional, pode ser vazio se usa só footerNav mobile)
        const c = $('#footer-container');
        if (!c) return;
        // FooterNav já cuida da navegação mobile
        // Este pode ser usado para footer desktop se necessário
    }
};
